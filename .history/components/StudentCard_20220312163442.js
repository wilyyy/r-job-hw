import styled from "styled-components";

const Container = styled.div``;

const StudentCard = ({
    city,
    company,
    email,
    firstName,
    lastName,
    grades
}) => {
    return (
        <Container>

        </Container>
    )
}

export default StudentCard;